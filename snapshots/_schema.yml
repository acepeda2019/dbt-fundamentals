snapshots:
  - name: orders_snapshot
    relation: source('jaffle_shop', 'orders')
    description: snapshot table of orders table
    config:
      schema: snapshots
      strategy: check
      unique_key: id
      check_cols:
        - ID
        - USER_ID
        - ORDER_DATE
        - STATUS
      hard_deletes: ignore
       